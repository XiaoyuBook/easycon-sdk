# Runtime/controller test support

This directory is reserved for the fake transport and deterministic vertical-slice harness added
with the controller implementation. It is not a release crate and must never read `EasyCon/`.
