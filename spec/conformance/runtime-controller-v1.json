{
  "schema_version": 1,
  "milestone": "runtime-controller-fake-v1",
  "license": "GPL-3.0-only",
  "scenarios": [
    {
      "id": "vertical-slice",
      "steps": [
        "create Runtime with VirtualClock",
        "subscribe to bounded events",
        "create fake Controller transport",
        "connect at 115200 with hello response",
        "submit direct action and observe transport acceptance",
        "submit precise sequence with absolute offsets",
        "cancel before a future step and accept neutral report",
        "close Runtime twice",
        "drain RuntimeClosed and close subscription",
        "attempt an event publish after RuntimeClosed"
      ],
      "assertions": [
        "operation states and events follow the canonical transitions",
        "report bytes and virtual timestamps match tracked fixtures",
        "direct success claims transport acceptance only",
        "active operation, task, and resource counts are zero after every successful close",
        "RuntimeClosed occurs after worker join and registry convergence and post-close event production is rejected"
      ]
    },
    {
      "id": "runtime-stabilization",
      "steps": [
        "spawn a supervised task through Runtime and call synchronous close from its task body",
        "close the same Runtime externally after the supervised task exits",
        "drop the final owning handle of a fresh Runtime with a live resource and subscription",
        "observe admission rejection and root cancellation without releasing the resource owner",
        "explicitly close another Runtime whose first resource callback panics and whose healthy resource can close",
        "hold the failed resource owner task in cleanup while concurrent close callers wait",
        "finish the real owner cleanup after every close caller receives the saved outcome",
        "call close again after CloseFailed"
      ],
      "assertions": [
        "supervised task close is rejected before Runtime state changes and external close later succeeds",
        "Runtime owns task completion, JoinHandle, owner binding, join, and unregister without a public task registration protocol",
        "final owning Drop rejects admission and requests root cancellation but runs no resource callback, starts no finalizer, and emits no RuntimeClosed",
        "resource callback panic does not prevent healthy resource cleanup",
        "all concurrent and later close callers receive one saved CloseFailed outcome and runtime.close_failed is the final event",
        "an operation remains non-terminal until its real owner cleanup and owner task exit",
        "CloseFailed retains diagnostic resource or task IDs and non-converged registry counts"
      ]
    },
    {
      "id": "operation-terminal-transaction",
      "steps": [
        "race parent terminal start against child operation admission",
        "register two cancellation hooks and a child where the first hook panics",
        "inject terminal event delivery failure while an operation waiter is blocked",
        "inject operation registry unlink failure while a second waiter is blocked",
        "poison each recoverable Runtime synchronization lock before retrying its operation"
      ],
      "assertions": [
        "child admission linearized after parent terminal sealing is rejected and an earlier child is cancelled",
        "one cancellation hook panic does not skip later hooks or any live child",
        "terminal state and payload are immutable and committed exactly once after owner cleanup",
        "registry unlink occurs before every terminal waiter returns",
        "event, registry, hook, and poisoned-lock faults cannot suppress waiter notification"
      ]
    },
    {
      "id": "timeout-separation",
      "steps": [
        "poll a running operation with zero wait timeout",
        "advance past an operation deadline without manually polling Runtime",
        "inject a handshake protocol timeout in a separate operation",
        "block an ACK wait until its protocol deadline",
        "block a report write until its independent I/O deadline"
      ],
      "assertions": [
        "wait timeout leaves the operation Running",
        "deadline requests cancellation and finishes Cancelled after cleanup",
        "protocol timeout finishes Failed with controller error",
        "a blocked ACK finishes Failed when its protocol deadline elapses",
        "write timeout finishes Failed with transport error"
      ]
    },
    {
      "id": "transport-faults",
      "steps": [
        "accept a report using partial writes",
        "ignore a late ACK from an older generation",
        "reject an error ACK",
        "queue an ACK behind a report that is waiting for its minimum interval",
        "cancel after direct and final sequence bytes are accepted but before write returns",
        "close immediately after an accepted report",
        "close while a report write is blocked before acceptance",
        "disconnect during an ACK and reconnect explicitly",
        "disconnect during a sequence",
        "close while an ACK read is blocked"
      ],
      "assertions": [
        "bytes are neither duplicated nor truncated",
        "late ACK cannot complete the current operation",
        "ACK waits for the earlier report instead of returning resource busy",
        "accepted-byte cancellation neutralizes and reaches Cancelled exactly once",
        "the close neutral report waits for the same minimum report interval",
        "close interrupts blocked write and joins the lane",
        "ACK disconnect resets desired state and closes transport before reconnect",
        "disconnect fails pending work and exposes neutralization warning",
        "transport close wakes the blocked read and the lane joins"
      ]
    },
    {
      "id": "event-overflow",
      "steps": [
        "create a capacity-one subscription",
        "publish more ordinary events than capacity",
        "retain an earlier critical event while a later ordinary event is dropped",
        "publish concurrently from multiple producers",
        "complete an operation",
        "read the subscription and query the operation",
        "close Runtime with a second subscription whose severity filter excludes Info"
      ],
      "assertions": [
        "EventGap reports the dropped sequence interval",
        "concrete events and gaps are read in strictly increasing sequence order",
        "producer never blocks",
        "authoritative terminal state remains queryable",
        "RuntimeClosed bypasses the severity filter and remains the final event"
      ]
    }
  ]
}
