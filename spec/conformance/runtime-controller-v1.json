{
  "schema_version": 1,
  "milestone": "runtime-controller-fake-v1",
  "license": "GPL-3.0-only",
  "scenarios": [
    {
      "id": "vertical-slice",
      "steps": [
        "create Runtime with VirtualClock",
        "subscribe to bounded events",
        "create fake Controller transport",
        "connect at 115200 with hello response",
        "submit direct action and observe transport acceptance",
        "submit precise sequence with absolute offsets",
        "cancel before a future step and accept neutral report",
        "close Runtime twice",
        "drain RuntimeClosed and close subscription",
        "attempt an event publish after RuntimeClosed"
      ],
      "assertions": [
        "operation states and events follow the canonical transitions",
        "report bytes and virtual timestamps match tracked fixtures",
        "direct success claims transport acceptance only",
        "active operation, task, and resource counts are zero after close",
        "post-close event production is rejected"
      ]
    },
    {
      "id": "timeout-separation",
      "steps": [
        "poll a running operation with zero wait timeout",
        "advance past an operation deadline without manually polling Runtime",
        "inject a handshake protocol timeout in a separate operation",
        "block an ACK wait until its protocol deadline",
        "block a report write until its independent I/O deadline"
      ],
      "assertions": [
        "wait timeout leaves the operation Running",
        "deadline requests cancellation and finishes Cancelled after cleanup",
        "protocol timeout finishes Failed with controller error",
        "a blocked ACK finishes Failed when its protocol deadline elapses",
        "write timeout finishes Failed with transport error"
      ]
    },
    {
      "id": "transport-faults",
      "steps": [
        "accept a report using partial writes",
        "ignore a late ACK from an older generation",
        "reject an error ACK",
        "queue an ACK behind a report that is waiting for its minimum interval",
        "cancel after direct and final sequence bytes are accepted but before write returns",
        "close while a report write is blocked before acceptance",
        "disconnect during an ACK and reconnect explicitly",
        "disconnect during a sequence",
        "close while an ACK read is blocked"
      ],
      "assertions": [
        "bytes are neither duplicated nor truncated",
        "late ACK cannot complete the current operation",
        "ACK waits for the earlier report instead of returning resource busy",
        "accepted-byte cancellation neutralizes and reaches Cancelled exactly once",
        "close interrupts blocked write and joins the lane",
        "ACK disconnect resets desired state and closes transport before reconnect",
        "disconnect fails pending work and exposes neutralization warning",
        "transport close wakes the blocked read and the lane joins"
      ]
    },
    {
      "id": "event-overflow",
      "steps": [
        "create a capacity-one subscription",
        "publish more ordinary events than capacity",
        "retain an earlier critical event while a later ordinary event is dropped",
        "publish concurrently from multiple producers",
        "complete an operation",
        "read the subscription and query the operation"
      ],
      "assertions": [
        "EventGap reports the dropped sequence interval",
        "concrete events and gaps are read in strictly increasing sequence order",
        "producer never blocks",
        "authoritative terminal state remains queryable"
      ]
    }
  ]
}
