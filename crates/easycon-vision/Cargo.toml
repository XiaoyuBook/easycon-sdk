[package]
name = "easycon-vision"
description = "Frames, labels, capture, and Vision scheduling for EasyCon SDK"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
base64 = "0.22"
easycon-model = { path = "../easycon-model" }
easycon-native-sys = { path = "../easycon-native-sys" }
easycon-runtime = { path = "../easycon-runtime" }
serde = "1.0"
serde_json = "1.0"

[lints]
workspace = true
